functions:
  lowercase-path: ../functions/lowercase-path.js

rules:
  api-path-lowercase:
    description: API Path must be kebab-case.
    severity: error
    given: $.paths[*]~
    then:
      function: lowercase-path
